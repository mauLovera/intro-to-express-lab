<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100&display=swap" rel="stylesheet">
  <title>Render eJS Training</title>
  <style>
    html {
      box-sizing: border-box;
    }
    *,
    *::after,
    *::before {
      box-sizing: inherit;
      /* font-family: 'Roboto'; */
    }
    body {
      color: white;
      background-color: black;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      width: 100vw;
    }
    h1 {
      text-transform: uppercase;
    }
    .wrapper {
      background: url("https://images4.alphacoders.com/707/707865.jpg") center;
      background-size: contain;
      background-repeat: no-repeat;
      height: 80%;
      width: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    .list {
      margin-right: 100px;
      align-self: flex-end;
      display: flex;
      flex-direction: column;
      list-style: none;
      gap: 2em;
      background-color: rgb(0, 0, 0);
      width: min-content;
      padding: 50px;
      border-top: 5px solid cyan;
      border-bottom: 5px solid red;
      font-size: 1.2em;
    }
    .survivor {
      color: rgb(0, 238, 255);
    }
    .killer {
      color: rgb(255, 0, 0);
    } 
  </style>
</head>
<body>
  <div class="wrapper">
  <ul class="list" >
    <h1>Dead by Daylight Characters:</h1>
    <% dbdChars.forEach(element => { %>
      <% if (element.faction === 'survivor') { %>
          <li class="survivor">
        <% } else { %>
            <li class="killer">
          <% } %>   
        <%= element.name %> 
        -
        <%= element.faction %>
      </li>
    <% }) %>
  </ul>
  </div>
</body>
</html>